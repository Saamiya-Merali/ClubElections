1. Download the file.
2. Put it in your folder of your choosing.
3. Run commands in order to run program:
   - `git add README.md`
   - `git commit -m "added short readme"`
   - `git push origin main`
